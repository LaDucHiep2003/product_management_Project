
mixin select-tree(items,level = 1,parent_id = "")
    each item in items
        - const prefix = Array(level + 1).join("--")
        option(
            value = item.id
            selected = (item.id == parent_id ? true : false)
        ) #{prefix}#{item.title}
            if item.childrent && item.childrent.length > 0
                +select-tree(item.childrent, level + 1,parent_id)