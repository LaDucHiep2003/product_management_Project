
mixin sub-menu(items, path)
    ul 

        each item in items
            li
                a(href = `${path}/${item.slug}`) #{item.title}
                if item.childrent && item.childrent.length > 0
                    +sub-menu(item.childrent, path)
